#ifndef HELPER_H
#define HELPER_H

#include <string>

std::string convertToNonAccentVN(std::string str);

// returns a string which contains only 'a' to 'z'
std::string normalize(std::string str);

#endif // HELPER_H
